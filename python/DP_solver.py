#has to be invariant to dimension, set the axis for which to solve, different types of cost functions

import numpy as np
import models as model


model
class DPSolver:
    def init(self, *, model = model.BaseModel, params = dict, )
        self.model = model

    

    
    